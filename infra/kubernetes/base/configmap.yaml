apiVersion: v1
kind: ConfigMap
metadata:
  name: leaderlib-config
  labels:
    app.kubernetes.io/name: leaderlib
    app.kubernetes.io/component: runtime-config
data:
  NODE_ENV: "production"
  PORT: "8080"
