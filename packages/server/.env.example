# Generate a new private key hex for local development:
# You can use: node -e "const { PrivateKey } = require('@bsv/sdk'); console.log(new PrivateKey().toString())"
# Or use this test key (DO NOT use in production):
LEADERLIB_PRIVATE_KEY_HEX=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Server port (default: 3001)
PORT=3001
